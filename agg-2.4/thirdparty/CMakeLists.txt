IF( NOT FT2_FOUND )
    IF ( agg_USE_FREETYPE )
        ADD_SUBDIRECTORY( freetype )
    ENDIF( agg_USE_FREETYPE )
ENDIF( NOT FT2_FOUND )

IF( NOT EXPAT_FOUND )
    IF ( agg_USE_EXPAT )
        ADD_SUBDIRECTORY( expat )
    ENDIF( agg_USE_EXPAT )
ENDIF( NOT EXPAT_FOUND )


